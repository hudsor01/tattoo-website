/**
 * Simple toast hook using sonner (same as hooks/use-toast)
 * This file exists for compatibility with components that import from ui/
 */
export { toast, useToast, type ToastOptions } from '@/hooks/use-toast';
